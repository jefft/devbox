version: "0.5"

processes:
  postgresql:
    command: "sh -c 'mkdir -p -m 700 \"$PGHOST\"; exec postgres -k \"$PGHOST\" -p \"${PGPORT:-5432}\"'"
    is_daemon: false
    shutdown:
      command: "pg_ctl stop -m fast"
    availability:
      restart: "always"
    readiness_probe:
      exec:
        command: "pg_isready -p \"${PGPORT:-5432}\""
      initial_delay_seconds: 2
